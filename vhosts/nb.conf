# vhost: ${sites:static28} - static28

server {
    listen *:${ports:http};
    server_name www.${hosts:static28};
    return 301 $scheme://${hosts:static28}$request_uri;
}

server {
    listen *:${ports:http};
    server_name ${hosts:static28};
    access_log ${buildout:directory}/var/log/${hosts:static28}.log;
    root ${static-locations:static28};
    # Set expires max on static file types (make sure you are using cache busting filenames or query params):
    location ~* ^.+\.(css|js|jpg|jpeg|gif|png|ico|gz|svg|svgz|ttf|otf|woff|eot|mp4|ogg|ogv|webm|pdf)$ {
      expires max;
      access_log off;
    }

    location  /index.html$ {
      expires 60m;
    }
}